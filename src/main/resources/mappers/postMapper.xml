<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.bookcafe.post.dao.PostDAO">
 
 
 <select id="selectPostList" parameterType="string"
  resultType="com.bookcafe.post.model.Post">

	SELECT
	id,
	userId,
	tag,
	title,
	content,
	imagePath,
	price,
	createdAt,
	updatedAt
	FROM
	post
	
	<if test="tag != null">
	WHERE
	tag = #{tag}
	
	</if>
	
	ORDER BY
	id
	DESC
 
 
 </select>
 
 
 
 
 
 <insert id="createPost" parameterType="map">

	INSERT
	INTO
	`post`
	(
	userId,
	tag,
	title,
	content,
	imagePath,
	price
	)
	VALUES
	(
	#{userId},
	#{tag},
	#{title},
	#{content},
	#{imagePath},
	#{price}

	)
 
 
 </insert>
 
 </mapper>